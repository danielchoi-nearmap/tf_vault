version: 2
jobs:
  build:
    docker:
      - image: nearmap/terraform-awscli:latest
    working_directory:  ~/tf_vault
    steps:
      - add_ssh_keys:
          fingerprints:
            "41:cc:09:01:8c:6b:56:ec:ac:da:e6:bb:98:0c:5d:e9"
      - checkout:
          path: ~/src/github.com/nearmap/tf_vault
      - run:
          name: install git
          command: |
            apt-get update
            apt-get install -y git
